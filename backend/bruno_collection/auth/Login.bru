meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "nombre_usuario": "inovello",
    "contrasenia": "Usuario.5"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
